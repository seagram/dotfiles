set -g default-terminal "tmux-256color"
set -sg terminal-overrides ",*:RGB"
set -g escape-time 0
set -g renumber-windows on
set -g mouse on
set -g extended-keys on
set -g extended-keys-format csi-u

set -g base-index 1
set -g pane-base-index 1

set -g status-position bottom
set -g status-justify left 
set -g status-style "bg=default"
set -g window-status-format         " #[fg=default,bold]#{window_name}:#[fg=#606079,nobold]#{b:pane_current_path}#[default] "
set -g window-status-current-format " #[fg=#6e94b2,bold]#{window_name}:#[fg=#8ba9c1,nobold]#{b:pane_current_path}#[default] "
set -g status-right ""
set -g status-left ""

unbind C-b
set -g prefix C-a
bind C-a send-prefix

bind r source-file ~/.config/tmux/tmux.conf \; display "reloaded tmux"

bind -n C-\= new-window -c "#{pane_current_path}"
bind -n C-\- confirm-before -p "kill-window #W? (y/n)" kill-window

bind -n C-\] next-window
bind -n C-\[ previous-window

bind -n C-\' split-window -h -c "#{pane_current_path}"
bind -n C-\; split-window -v -c "#{pane_current_path}"

bind -n C-\, select-pane -L
bind -n C-\. select-pane -R

bind -r C-j resize-pane -D 15
bind -r C-k resize-pane -U 15
bind -r C-h resize-pane -L 15
bind -r C-l resize-pane -R 15

set -g focus-events on
set -g status-style bg=default
set -g status-left-length 99
set -g status-right-length 99
set -g status-justify left
